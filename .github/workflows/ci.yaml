name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: echo ">> Hello GitHub Actions I'm building now ..."

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - run: echo ">> I'm testing now"  